/**
 * Size of the result buffer in bytes (16KB).
 * This buffer is used for SharedArrayBuffer communication between
 * the main thread and worker thread.
 */
export const RESULT_BUFFER_SIZE = 16000;
